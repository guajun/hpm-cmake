name: Firmware environment
on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-22.04, ubuntu-24.04]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
      - name: Maintainer-provided test SDK and tools
        run: python tests/prepare.py
      - name: Local-only integration and native build
        run: python tests/integration.py
      - name: Verify website command generation
        run: node tests/command.test.cjs
